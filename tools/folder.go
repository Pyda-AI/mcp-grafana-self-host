package tools

import (
	"github.com/mark3labs/mcp-go/server"
)

// AddFolderTools registers folder read tools with the MCP server.
// Currently there are no read-only folder tools available.
func AddFolderTools(mcp *server.MCPServer) {
	// No read-only folder tools available
}
